# === CẤU HÌNH EMAIL — Gửi Giấy đề nghị xe ===
# Đăng ký Brevo miễn phí tại https://brevo.com
# Vào SMTP & API → SMTP keys → Create a new SMTP key
SMTP_HOST=smtp-relay.brevo.com
SMTP_PORT=587
SMTP_USER=email_cua_ban@gmail.com
SMTP_PASS=xkeys-... (lấy từ Brevo)

# Email người nhận mặc định (có thể nhập tay trên dialog)
SMTP_RECIPIENT=nguoinhan@vietinbank.vn

# === SUPABASE (đã có sẵn) ===
NEXT_PUBLIC_SUPABASE_URL=https://...
SUPABASE_SERVICE_ROLE_KEY=...
